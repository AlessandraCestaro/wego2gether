<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-4" style="text-align: center">

        <h1>Ciao <%= current_invited_user.first_name %>!</h1>
          <h2>We are travelling</h2>
          <h2>Join us</h2>
          <h2><%= @trip.name %></h2>
          <br>
          <br>
          <%= simple_form_for [@trip, @user_trip] do |f| %>
            <%= f.button :submit, name: "user_trip[state]", value: "ACCEPT" %>
            <%= f.button :submit, name: "user_trip[state]", value: "DECLINE" %>
          <% end %>

    </div>
  </div>
</div>
